// This is a high-scale full-stack nonprofit website with frontend (React) and backend (Node.js + Express + MongoDB)
// Pages: Home, Blog, Podcast, Store. Includes API routes and database integration.

// Frontend Structure: React + Tailwind CSS + React Router
// Backend Structure: Node.js + Express + MongoDB
// Store: Stripe for payment processing (mock integration for now)

// /frontend/src/App.jsx
import React from "react";
import { BrowserRouter as Router, Routes, Route } from "react-router-dom";
import HomePage from "./pages/Home";
import BlogPage from "./pages/Blog";
import PodcastPage from "./pages/Podcast";
import StorePage from "./pages/Store";
import NavbarComponent from "./components/Navbar";

function App() {
  return (
    <Router>
      <div className="min-h-screen bg-gray-50 text-gray-800">
        <NavbarComponent />
        <Routes>
          <Route path="/" element={<HomePage />} />
          <Route path="/blog" element={<BlogPage />} />
          <Route path="/podcast" element={<PodcastPage />} />
          <Route path="/store" element={<StorePage />} />
        </Routes>
      </div>
    </Router>
  );
}

export default App;

// /frontend/src/pages/Home.jsx
import React from "react";
const HomePage = () => {
  return <div className="p-6">Welcome to our Nonprofit Home Page!</div>;
};
export default HomePage;

// /frontend/src/pages/Blog.jsx
import React from "react";
const BlogPage = () => {
  return (
    <div className="p-6 space-y-4">
      <h1 className="text-2xl font-bold">Blog Articles</h1>
      <article className="bg-white p-4 shadow rounded">
        <h2 className="text-xl font-semibold">Article One</h2>
        <p>Lorem ipsum dolor sit amet, consectetur adipiscing elit...</p>
      </article>
      <article className="bg-white p-4 shadow rounded">
        <h2 className="text-xl font-semibold">Article Two</h2>
        <p>Ut enim ad minim veniam, quis nostrud exercitation ullamco...</p>
      </article>
    </div>
  );
};
export default BlogPage;

// /frontend/src/pages/Podcast.jsx
import React from "react";
const PodcastPage = () => {
  return <div className="p-6">Welcome to our Podcast page!</div>;
};
export default PodcastPage;

// /frontend/src/pages/Store.jsx
import React from "react";
const StorePage = () => {
  return <div className="p-6">Support our mission — visit the store!</div>;
};
export default StorePage;

// /frontend/src/components/Navbar.jsx
import React from "react";
import { Link } from "react-router-dom";

const NavbarComponent = () => {
  return (
    <nav className="bg-white shadow p-4 flex justify-between">
      <div className="text-lg font-bold">Nonprofit</div>
      <div className="space-x-4">
        <Link to="/" className="hover:underline">Home</Link>
        <Link to="/blog" className="hover:underline">Blog</Link>
        <Link to="/podcast" className="hover:underline">Podcast</Link>
        <Link to="/store" className="hover:underline">Store</Link>
      </div>
    </nav>
  );
};
export default NavbarComponent;

// Backend: server.js
// /backend/server.js
const express = require("express");
const mongoose = require("mongoose");
const cors = require("cors");
require("dotenv").config();

const app = express();
const PORT = process.env.PORT || 5000;

app.use(cors());
app.use(express.json());

// Connect to MongoDB
mongoose.connect(process.env.MONGO_URI, {
  useNewUrlParser: true,
  useUnifiedTopology: true,
});

// API Routes
const blogRoutes = require("./routes/blogRoutes");
const podcastRoutes = require("./routes/podcastRoutes");
const storeRoutes = require("./routes/storeRoutes");

app.use("/api/blogs", blogRoutes);
app.use("/api/podcasts", podcastRoutes);
app.use("/api/store", storeRoutes);

app.get("/", (req, res) => {
  res.send("Welcome to the Nonprofit API");
});

app.listen(PORT, () => console.log(`Server running on port ${PORT}`));
